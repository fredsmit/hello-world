import { getRequiredHTMLElements } from "./pageUtils.js"
import { addTooltipHandler } from "./t27.js"

//const { house, roof } = getRequiredHTMLElements("house", "roof");
//addTooltipHandler(house);

addTooltipHandler(document.body);

export { };
